// Slidev auto-loads this file as a global stylesheet entry.
// Rules here are NOT Vue-scoped, so they reach across all slides
// and can target Slidev's own wrapper elements (.slidev-slide-content, etc.).
import './global.css'
